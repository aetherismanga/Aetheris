const $=s=>document.querySelector(s);
const heroImg='https://images.unsplash.com/photo-1763485956292-6fb531f01b0c?auto=format&fit=crop&w=1800&q=85';
const diagImg='https://images.unsplash.com/photo-1543525469-65b61cc2bc06?auto=format&fit=crop&w=1400&q=85';
const images={
  Carrelage:'https://images.unsplash.com/photo-1782805153077-0e348cc5409d?auto=format&fit=crop&w=1400&q=85',
  Peinture:'https://images.unsplash.com/photo-1693985120993-e9b203ce7631?auto=format&fit=crop&w=1400&q=85',
  'Sol / parquet':'https://images.unsplash.com/photo-1743385199973-0f867a26c833?auto=format&fit=crop&w=1400&q=85',
  'Placo / isolation':'https://images.unsplash.com/photo-1768321902114-12b1777549a6?auto=format&fit=crop&w=1400&q=85'
};
const meta={
  Carrelage:['Carrelage','Préparation, calepinage, pose et joints.'],
  Peinture:['Peinture','Préparer, protéger et appliquer dans le bon ordre.'],
  'Sol / parquet':['Sol / parquet','Sous-couche, pose et finitions.'],
  'Placo / isolation':['Placo / isolation','Rails, isolant, plaques et bandes.']
};
const starter={id:1,title:'Salle de bain invités',category:'Carrelage',area:18,budget:890,steps:[['Protection et dépose',1],['Préparation des supports',1],['Étanchéité',0],['Pose du carrelage',0],['Joints et finitions',0]],materials:[['Carrelage mural','19,8 m²',1],['Colle C2S1','4 sacs',0],['Joint hydrofuge','5 kg',0],['Croisillons autonivelants','250 pièces',1]]};
let projects=[];try{projects=JSON.parse(localStorage.getItem('bricoach-projects')||'null')||[starter]}catch(e){projects=[starter]}
let tab='home',photo='',diagnosed=false,problem='Humidité / moisissure';
let messages=[['bot','Bonjour 👋 Je suis ton Coach chantier. Pose-moi une question sur ton projet, les matériaux ou l’ordre des travaux.']];
const nav=[['home','🏠','Accueil'],['projects','📋','Chantiers'],['calc','🧮','Calculs'],['diagnostic','📸','Photo'],['coach','💬','Coach']];
function progress(p){return p.steps?.length?Math.round(p.steps.filter(x=>x[1]).length/p.steps.length*100):0}
function img(cat){return images[cat]||heroImg}
function save(){localStorage.setItem('bricoach-projects',JSON.stringify(projects))}
function go(t){tab=t;render()}
function shell(content){return `<div class="shell"><aside class="side"><div class="brand"><span class="logo">🔨</span>Bricoach</div><div class="tag">Ton chantier, étape par étape</div><div class="nav">${nav.map(n=>`<button class="${tab===n[0]?'active':''}" onclick="go('${n[0]}')"><span>${n[1]}</span>${n[2]}</button>`).join('')}</div><div class="sideFoot"><b>💡 Conseil du Coach</b><br>Avance une étape à la fois et coche ce qui est terminé.</div></aside><main class="main"><div class="top"><div><div class="eyebrow">MON COACH TRAVAUX</div><h1>${tab==='home'?'Bonjour 👋':nav.find(n=>n[0]===tab)[2]}</h1></div><div class="avatar">BC</div></div>${content}</main><nav class="bottom">${nav.map(n=>`<button class="${tab===n[0]?'active':''}" onclick="go('${n[0]}')"><span>${n[1]}</span>${n[2]}</button>`).join('')}</nav></div>`}
function home(){let p=projects[0];return `<section class="hero"><div class="heroCopy"><div class="eyebrow heroEye">TON CHANTIER, BIEN ACCOMPAGNÉ</div><h2>Planifie. Réalise.<br><span>Profite.</span></h2><p>Décris ton projet et Bricoach t’accompagne : ordre des travaux, quantités, budget, liste de courses et conseils quand tu bloques.</p><div class="heroActions"><button class="btn primary" onclick="newProject()">＋ Démarrer un chantier</button><button class="btn glass" onclick="go('diagnostic')">📸 J’ai un problème</button></div><div class="heroTrust"><span>✓ Plans de chantier</span><span>✓ Calculs matériaux</span><span>✓ Coach travaux</span></div></div><div class="heroVisual"><img src="${heroImg}" alt="Rénovation intérieure Bricoach"><div class="heroFloat"><span class="floatIcon">✓</span><div><b>${projects.length} chantier${projects.length>1?'s':''} en cours</b><small>Reprends exactement où tu t’es arrêté.</small></div></div></div></section><section class="sectionBlock"><div class="sectionTitle"><div><div class="eyebrow">CHOISIS TON PROJET</div><h2>Un accompagnement adapté au chantier</h2></div><div class="muted">Chaque métier a ses propres étapes, calculs et conseils.</div></div><div class="tradeGrid">${Object.entries(meta).map(([cat,m])=>`<button class="tradeCard" onclick="newProject('${cat}')"><img src="${img(cat)}" loading="lazy" alt="${m[0]}"><span class="tradeShade"></span><span class="tradeText"><b>${m[0]}</b><small>${m[1]}</small></span></button>`).join('')}</div></section><div class="grid"><div class="card quick visualQuick" onclick="go('diagnostic')"><img src="${diagImg}" loading="lazy" alt="Diagnostic chantier"><div class="quickOverlay"><div class="quickIcon">📸</div><div><h3>Montre-moi ton problème</h3><div>Photo + diagnostic guidé</div></div></div></div><div class="card quick" onclick="go('calc')"><div class="quickIcon">🧮</div><div><h3>Calculer mes matériaux</h3><div class="muted">Quantités, pertes et liste de courses</div></div></div><div class="card quick" onclick="go('coach')"><div class="quickIcon">💬</div><div><h3>Demander au Coach</h3><div class="muted">Une question sur ton chantier ?</div></div></div>${p?`<div class="card project featuredProject"><div class="projectMedia"><img src="${img(p.category)}" alt="${p.category}"><span class="projectMediaShade"></span><div class="projectMediaTitle"><span class="pill">${p.category}</span><h3>${p.title}</h3></div></div><div class="projectBody"><div class="sectionHead"><div><div class="muted small">CHANTIER PRIORITAIRE</div><b>${progress(p)}% terminé</b></div><button class="btn soft" onclick="go('projects')">Continuer →</button></div><div class="progress"><span style="width:${progress(p)}%"></span></div><div class="steps">${p.steps.slice(0,4).map((s,i)=>`<div class="step"><span class="dot ${s[1]?'done':''}">${s[1]?'✓':i+1}</span>${s[0]}</div>`).join('')}</div></div></div><div class="card stats"><h3>Vue rapide</h3><div class="metric"><span class="muted">⏱ Avancement</span><b>${progress(p)}%</b></div><div class="metric"><span class="muted">€ Budget</span><b>${p.budget} €</b></div><div class="metric"><span class="muted">☑ Étapes</span><b>${p.steps.filter(x=>x[1]).length}/${p.steps.length}</b></div></div>`:''}</div>`}
function projectsPage(){return `<div class="sectionHead pageTitle"><div><h2>Mes chantiers</h2><div class="muted">Avance étape par étape sans rien oublier.</div></div><button class="btn primary" onclick="newProject()">＋ Nouveau chantier</button></div><div class="cards2">${projects.map(p=>`<div class="card projectCard"><div class="projectMedia compact"><img src="${img(p.category)}" loading="lazy" alt="${p.category}"><span class="projectMediaShade"></span><div class="projectMediaTitle"><span class="pill">${p.category}</span><h3>${p.title}</h3></div></div><div class="projectBody"><div class="sectionHead"><span class="muted">${p.area} m² · budget ${p.budget} €</span><b>${progress(p)}%</b></div><div class="progress"><span style="width:${progress(p)}%"></span></div><div class="steps">${p.steps.map((s,i)=>`<button class="step" onclick="toggleStep(${p.id},${i})"><span class="dot ${s[1]?'done':''}">${s[1]?'✓':i+1}</span>${s[0]}</button>`).join('')}</div></div></div>`).join('')}</div>`}
function toggleStep(id,i){let p=projects.find(x=>x.id===id);if(!p)return;p.steps[i][1]=p.steps[i][1]?0:1;save();render()}
function calcPage(){return `<div class="pageTitle"><h2>Calculateur matériaux</h2><div class="muted">Une estimation simple et transparente.</div></div><div class="cards2"><div class="card calcCard"><div class="calcCover"><img src="${images.Carrelage}" alt="Carrelage"></div><h3>Calcul express carrelage</h3><div class="field"><label>Surface nette (m²)</label><input id="surface" type="number" value="20" oninput="recalc()"></div><div class="field"><label>Marge de perte (%)</label><input id="loss" type="number" value="10" oninput="recalc()"></div><div id="calcResult" class="diagnostic"></div></div><div class="card"><h3>Listes de courses</h3>${projects.map(p=>`<div class="shopping"><b>${p.title}</b>${(p.materials||[]).map((m,i)=>`<label class="shopItem ${m[2]?'done':''}"><input type="checkbox" ${m[2]?'checked':''} onchange="toggleMat(${p.id},${i})"><span>${m[0]}</span><b>${m[1]}</b></label>`).join('')}</div>`).join('')}</div></div>`}
function recalc(){let s=Number($('#surface')?.value||20),l=Number($('#loss')?.value||10),order=s*(1+l/100),glue=Math.ceil(s/5),el=$('#calcResult');if(el)el.innerHTML=`<b>À commander : ${order.toFixed(1)} m²</b><br><span class="muted">Colle estimative : ${glue} sacs de 25 kg. À ajuster selon format, support et rendement fabricant.</span>`}
function toggleMat(id,i){let p=projects.find(x=>x.id===id);if(!p||!p.materials?.[i])return;p.materials[i][2]=p.materials[i][2]?0:1;save();render()}
function diagPage(){return `<div class="pageTitle"><h2>📸 Montre-moi ton chantier</h2><div class="muted">Ajoute une photo puis choisis le type de problème.</div></div><div class="diagHero"><img src="${diagImg}" alt="Diagnostic chantier"><div><div class="eyebrow">ASSISTANCE CHANTIER</div><h3>Un problème ? Commence par observer avant de réparer.</h3><p>Bricoach structure les vérifications et te signale les situations où un professionnel est préférable.</p></div></div><div class="cards2"><div class="card"><div class="upload"><div class="camera">📷</div><h3>Prendre ou ajouter une photo</h3><input type="file" accept="image/*" capture="environment" onchange="loadPhoto(event)">${photo?`<img class="preview" src="${photo}">`:''}</div><div class="field"><label>Type de problème</label><select id="problem" onchange="problem=this.value;diagnosed=false"><option>Humidité / moisissure</option><option>Fissure</option><option>Carrelage décollé</option><option>Fuite d’eau</option><option>Électricité</option></select></div><button class="btn primary full" ${photo?'':'disabled'} onclick="diagnosed=true;render()">✨ Analyser le problème</button></div><div class="card"><h3>Diagnostic guidé</h3>${diagnosed?diagnosticText():`<div class="empty">Le résultat apparaîtra ici après l’ajout d’une photo.</div>`}</div></div>`}
function loadPhoto(e){let f=e.target.files?.[0];if(!f)return;let r=new FileReader();r.onload=()=>{photo=r.result;diagnosed=false;render()};r.readAsDataURL(f)}
function diagnosticText(){let t='Contrôle recommandé',b='Observe la zone et identifie la cause avant toute réparation définitive.',danger=false;if(problem.includes('Humidité')){t='Traces compatibles avec un problème d’humidité';b='Vérifie si la trace augmente après la pluie, s’il existe une canalisation proche et si la ventilation est correcte.'}if(problem==='Fissure'){t='Fissure à qualifier';b='Mesure sa largeur et surveille son évolution. Une fissure active, large ou traversante mérite un avis professionnel.'}if(problem.includes('Carrelage')){t='Décollement possible';b='Teste doucement les carreaux voisins. S’ils bougent ou sonnent creux, contrôle le support et le collage.'}if(problem.includes('Fuite')){t='Suspicion de fuite';b='Coupe l’eau si la fuite est active et identifie l’origine avant toute remise en état.';danger=true}if(problem.includes('Électricité')){t='Intervention électrique';b='Coupe le circuit au tableau et ne démonte pas une installation sous tension.';danger=true}return `<div class="diagnostic ${danger?'danger':''}"><b>${danger?'⚠️':'🔎'} ${t}</b><p>${b}</p><div class="small">Diagnostic indicatif : ne remplace pas le contrôle d’un professionnel lorsqu’il existe un risque.</div></div>`}
function coachPage(){return `<div class="pageTitle"><h2>Coach chantier</h2><div class="muted">Le Coach utilise le contexte du chantier principal.</div></div><div class="card"><div class="chat" id="chat">${messages.map(m=>`<div class="bubble ${m[0]}">${m[1]}</div>`).join('')}</div><form class="chatForm" onsubmit="coach(event)"><input id="msg" placeholder="Ex : quelle colle pour ma douche ?"><button class="btn primary">Envoyer</button></form></div>`}
function coach(e){e.preventDefault();let q=$('#msg').value.trim();if(!q)return;let p=projects[0]||starter,l=q.toLowerCase(),a='Avance étape par étape et vérifie les préconisations du fabricant.';if(l.includes('colle'))a=`Pour ${p.category.toLowerCase()}, choisis la colle selon le support, le format et l’usage. Ton chantier fait ${p.area} m².`;if(l.includes('étanch'))a='En douche, traite angles, jonctions et passages de tuyaux puis respecte le temps de séchage.';if(l.includes('budget'))a=`Le budget enregistré pour « ${p.title} » est de ${p.budget} €. Prévois 10 à 15 % de marge.`;if(l.includes('peinture'))a='Support propre, sec et stable : rebouchage, ponçage, dépoussiérage puis sous-couche si nécessaire.';messages.push(['user',q],['bot',a]);render();setTimeout(()=>{$('#chat').scrollTop=$('#chat').scrollHeight},0)}
function newProject(cat=''){$('#app').insertAdjacentHTML('beforeend',`<div class="modalBack" id="modal"><div class="modal"><div class="sectionHead"><div><div class="eyebrow">NOUVEAU CHANTIER</div><h2>Décris ton projet</h2></div><button class="close" onclick="$('#modal').remove()">✕</button></div><div class="field"><label>Nom du chantier</label><input id="ntitle" placeholder="Ex : Salle de bain principale"></div><div class="field"><label>Type de travaux</label><select id="ncat"><option>Carrelage</option><option>Peinture</option><option>Sol / parquet</option><option>Placo / isolation</option></select></div><div class="cards2"><div class="field"><label>Surface (m²)</label><input id="narea" type="number" value="12"></div><div class="field"><label>Budget cible (€)</label><input id="nbudget" type="number" value="650"></div></div><button class="btn primary full" onclick="createProject()">✨ Générer mon plan chantier</button></div></div>`);if(cat)$('#ncat').value=cat}
function createProject(){let title=$('#ntitle').value||'Mon nouveau chantier',cat=$('#ncat').value,a=Number($('#narea').value)||1,b=Number($('#nbudget').value)||0;let steps={Carrelage:['Protection et préparation','Traçage du calepinage','Préparation de la colle','Pose du carrelage','Joints et finitions'],Peinture:['Protection','Lessivage / rebouchage','Ponçage','Sous-couche','2 couches de finition'],'Sol / parquet':['Contrôle du support','Sous-couche','Calepinage','Pose du sol','Plinthes et finitions'],'Placo / isolation':['Traçage','Pose des rails','Montants et isolant','Plaques','Bandes et enduit']}[cat];let mats=cat==='Carrelage'?[['Carrelage',`${(a*1.1).toFixed(1)} m²`,0],['Colle',`${Math.ceil(a/5)} sacs`,0],['Joint',`${Math.max(2,Math.ceil(a*.25))} kg`,0],['Croisillons',`${Math.ceil(a*18)} pièces`,0]]:cat==='Peinture'?[['Peinture',`${Math.ceil(a*2/10)} pots de 10 L`,0],['Sous-couche',`${Math.ceil(a/8)} pots`,0],['Enduit',`${Math.ceil(a/10)*5} kg`,0]]:cat==='Sol / parquet'?[['Revêtement',`${(a*1.08).toFixed(1)} m²`,0],['Sous-couche',`${Math.ceil(a)} m²`,0],['Plinthes',`${Math.ceil(Math.sqrt(a)*4*1.1)} ml`,0]]:[['Plaques BA13',`${Math.ceil(a/3)} plaques`,0],['Montants',`${Math.ceil(a*.9)} pièces`,0],['Rails',`${Math.ceil(a*.4)} pièces`,0],['Enduit',`${Math.ceil(a*.35)} kg`,0]];projects.unshift({id:Date.now(),title,category:cat,area:a,budget:b,steps:steps.map(x=>[x,0]),materials:mats});save();tab='projects';render()}
function render(){let c=tab==='home'?home():tab==='projects'?projectsPage():tab==='calc'?calcPage():tab==='diagnostic'?diagPage():coachPage();let app=$('#app');if(app)app.innerHTML=shell(c);if(tab==='calc')setTimeout(recalc,0)}
window.addEventListener('error',()=>{let app=$('#app');if(app&&!app.innerHTML.trim())app.innerHTML='<div style="padding:24px;font-family:system-ui"><h2>Bricoach</h2><p>Une erreur de chargement est survenue. Recharge la page.</p></div>'});
render();